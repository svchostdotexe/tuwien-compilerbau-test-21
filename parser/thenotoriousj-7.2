class J implements:
	var cool: Foo;
	var uncool: Bar;

	method move(tmp: int, jeppo: Foo)    
		if 0 < 1 < 2 then 
			55fH; /* just a stat */
			return (1 + 2) * 3;
		else 
			if tmp and jeppo then
				cool := null g; 
			end;
		end;
		return this;
    end;
end;